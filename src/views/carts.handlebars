<main
  class="min-w-full bg-[#fdf6f6] flex justify-center items-center mt-[70px] flex-col"
>
  <div class="w-full flex, justify-start">

    <h1 class="font-bold">Carrito ID: {{_id}}</h1>
  </div>

  <div>
    {{#each products}}

      <p> ID:{{this._id}}</p>
      <p>Cantidad: {{this.quantity}}</p>
      <div class="flex justify-center gap-4">

      </div>

      <div class="flex gap-5 items-center">
        <div class="w-11">
          <img src="{{this.product.thumbnail}}" alt="{{this.product.title}}" />
        </div>
        <p>Categoría: {{this.product.category}}</p>
        <p>Nombre: {{this.product.title}}</p>
        <p>Precio: {{this.product.price}}</p>
      </div>

    {{/each}}
  </div>
</main>